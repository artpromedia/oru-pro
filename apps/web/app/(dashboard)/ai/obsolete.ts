export const obsoleteInOonru = {
  tCodes: "Natural language replaces transaction codes",
  menuNavigation: "AI understands intent directly",
  manualDataEntry: "AI captures from context/sensors",
  reportGeneration: "AI creates on-demand insights",
  approvalWorkflows: "AI pre-approves within parameters",
  masterDataMaintenance: "AI keeps data current via learning"
};

export type ObsoleteKey = keyof typeof obsoleteInOonru;
